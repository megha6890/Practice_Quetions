 int minValue(Node root) {
        if(root==null)
        return 0;
        
        while(root!=null && root.left!=null){
            root=root.left;
        }
        return root.data;
        // code here
    }
